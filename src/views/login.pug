extends layouts/main

block content
    .form-container
        h1.form__title Login
        form(action=routes.login, method="post")
            input.input__text(type="email", name="email", placeholder="E-mail", required)
            input.input__text(type="password", name="password", placeholder="Password", required)
            input(type="submit", value="Login")