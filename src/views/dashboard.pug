extends layouts/main
include mixins/thumbnail

block content
    .dashboard
        each image in images
            +thumbnail({
                id: image.id,
                src: image.fileURL
            })